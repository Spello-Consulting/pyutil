[project]
name = "pyutil"
version = "1.0.1"
description = "Shared shell tooling and dev-environment scripts."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
